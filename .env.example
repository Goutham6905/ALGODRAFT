# AlgoDraft Environment Configuration

# Backend Server Configuration
LOG_LEVEL=INFO
CHROMA_DIR=./backend/chroma_db

# CORS Configuration (comma-separated origins, or * for all)
ALLOWED_ORIGINS=*

# API Key for cloud providers
# This can be set here or in the config.json file via the UI
ALGODRAFT_API_KEY=

# Note: The system supports multiple cloud providers:
# - OpenAI: Use sk-... API keys (from https://platform.openai.com/account/api-keys)
# - Anthropic: Use sk-ant-... API keys (from https://console.anthropic.com/)
# - Hugging Face: Use hf_... API keys (from https://huggingface.co/settings/tokens)
#
# Configure the provider and API key through the AlgoDraft VS Code extension settings panel.
